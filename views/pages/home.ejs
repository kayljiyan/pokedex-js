<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("../partials/header") %>
</head>
<body class="bg-red-600 text-white min-h-screen flex flex-col items-center p-5 pb-16">
    <div class="flex-grow pb-4">
  <div class="max-w-4xl w-full bg-gray-900 rounded-3xl shadow-2xl p-6 border-4 border-yellow-400">
    <h1 class="text-4xl font-bold text-center mb-5 tracking-wide">Pokédex</h1>

    <!-- 🔍 Search Form -->
    <form action="/search" method="GET" class="flex justify-center mb-6">
      <input type="text" name="name" placeholder="Search Pokémon..." class="px-4 py-2 border-2 border-yellow-400 rounded-l-full text-gray-900 focus:outline-none">
      <button type="submit" class="bg-yellow-400 text-gray-900 font-bold px-6 py-2 rounded-r-full border-2 border-yellow-400 hover:bg-yellow-500 transition">Search</button>
    </form>

    <!-- Pokémon List -->
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
      <% pokemonList.forEach(pokemon => { %>
        <a href="/pokemon/<%= pokemon.name %>" class="p-4 bg-gray-800 border-2 border-yellow-400 rounded-xl text-center shadow-lg hover:shadow-2xl transform hover:scale-105 transition">
          <p class="text-xl capitalize font-semibold text-yellow-300 tracking-wide"><%= pokemon.name %></p>
        </a>
      <% }); %>
    </div>
  </div>
  </div>
  <%- include("../partials/footer") %>
</body>
</html>

